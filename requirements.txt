# BREACH.AI Enterprise Dependencies

# AI & LLM
anthropic>=0.18.0

# HTTP & Networking
httpx>=0.27.0
aiohttp>=3.9.0
dnspython>=2.5.0
aiofiles>=23.0.0

# HTML/XML Parsing
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Data Validation
pydantic>=2.0.0

# Terminal UI
rich>=13.0.0

# Templating (Reports)
jinja2>=3.1.0

# PDF Generation (optional - install one of these)
# weasyprint>=60.0      # Best quality, requires system deps
# xhtml2pdf>=0.2.11     # Pure Python, easier install
reportlab>=4.0.0       # Direct PDF generation

# Utilities
python-dateutil>=2.8.0

# ============== ENTERPRISE BACKEND ==============

# FastAPI & Server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# Database
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0          # PostgreSQL async driver
aiosqlite>=0.19.0        # SQLite async (for dev)
alembic>=1.13.0          # Migrations

# Authentication
pyjwt>=2.8.0
passlib[bcrypt]>=1.7.4
python-jose[cryptography]>=3.3.0

# Billing (Stripe)
stripe>=7.0.0

# Rate Limiting
slowapi>=0.1.9

# Email
aiosmtplib>=3.0.0

# Caching (optional)
redis>=5.0.0

# Configuration
pydantic-settings>=2.0.0

# Job Queue
arq>=0.26.0

# Scheduling
croniter>=2.0.0          # Cron expression parsing

# Vector Database
qdrant-client>=1.7.0

# Cloud Storage (Cloudflare R2 / S3)
aioboto3>=12.0.0

# Environment
python-dotenv>=1.0.0

# Observability
structlog>=24.0.0
prometheus-client>=0.20.0
sentry-sdk[fastapi]>=1.40.0

# ============== DEVELOPMENT ==============
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.0.0
black>=24.0.0
ruff>=0.2.0
mypy>=1.8.0
httpx>=0.27.0  # For testing

# Load Testing
locust>=2.20.0
