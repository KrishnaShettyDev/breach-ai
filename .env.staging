# BREACH.AI - Staging Configuration Template
# For preview/staging deployments (test production before going live)

# ============== DATABASE ==============
# Use a separate staging database (not production!)
DATABASE_URL=postgresql+asyncpg://user:password@staging-host.neon.tech/breach_staging?sslmode=require

# ============== REDIS ==============
REDIS_URL=rediss://default:xxx@staging.upstash.io:6379

# ============== VECTOR DB (Qdrant) ==============
# Can use local or cloud staging instance
QDRANT_URL=http://localhost:6333
# For cloud:
# QDRANT_URL=https://staging-xxx.qdrant.io:6333
# QDRANT_API_KEY=xxx

# ============== CLERK AUTH ==============
# Use TEST keys for staging (same as dev)
CLERK_PUBLISHABLE_KEY=pk_test_xxx
CLERK_SECRET_KEY=sk_test_xxx

# ============== AI ==============
ANTHROPIC_API_KEY=sk-ant-xxx
# OPENAI_API_KEY=sk-xxx

# ============== STRIPE BILLING ==============
# Use TEST keys for staging
STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx

# Price IDs from your Stripe Products (test mode)
STRIPE_STARTER_PRICE_ID=price_xxx
STRIPE_BUSINESS_PRICE_ID=price_xxx
STRIPE_ENTERPRISE_PRICE_ID=price_xxx

# ============== SERVER ==============
HOST=0.0.0.0
PORT=8000
DEBUG=true
ENVIRONMENT=staging

# ============== CORS ==============
# Your staging frontend URL
CORS_ORIGINS=https://staging.breach.yourdomain.com

# ============== SCANNING ==============
SCAN_TIMEOUT_SECONDS=3600
MAX_CONCURRENT_SCANS=3

# ============== RATE LIMITING ==============
RATE_LIMIT_GLOBAL=100/minute
RATE_LIMIT_SCANS=10/minute
RATE_LIMIT_AUTH=5/minute

# ============== OBSERVABILITY ==============
# Sentry (use staging project if available)
# SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx
SENTRY_TRACES_SAMPLE_RATE=0.5

# Alerts (optional)
# ALERT_WEBHOOK_URL=https://hooks.slack.com/services/xxx
